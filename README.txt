To test execution complete the next steps:

* Install node.js 20.9.0 version

* Install playwright (during installation select default settings): 
npm init playwright@latest

* Execute tests:
npx playwright test